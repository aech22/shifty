# VISION.md — Shifty

作成日: 2026-08-10（BACKLOG「バグチェック#44 申し送り」の一項目として新規作成）

このファイルは `/bug-check`（バグチェックループ）と `/shifty-feature`（機能実装ループ）が
**PHASE 0 で読み込み、完了基準として参照する**正本である。両ループはここに書かれた基準を満たした時点で
「完了」と判定してよい。基準を変えるときはこのファイルを更新する。

関連: [CLAUDE.md](CLAUDE.md)（アーキテクチャ）／ [RULES.md](RULES.md)（禁止事項）／ [BACKLOG.md](BACKLOG.md)（実装待ち）

---

## プロダクトの目的

**飲食店のシフト希望収集と、そこから先の「シフト表を仕上げる」作業を、1つのURLで完結させる。**

シフト提出は紙・LINE・口頭に分散しがちで、集計と清書は毎月まったく同じ手作業として再発する。
Shifty はこの「毎回同じ工数」を無くすことを目的とする。

- **スタッフ**: URLを開くだけ。アプリのインストールもアカウント作成も要求しない。
- **管理者**: 提出状況の把握 → 出退勤の調整 → シフト表の完成 → Excel/PDF 出力までをアプリ内で終える。

## ターゲット

飲食店（個人店〜数十店舗の企業）のシフト作成担当者。スマートフォンでの利用を前提とする。

## プラン

| プラン | 内容 | 価格（税込） |
|---|---|---|
| Free | スタッフ20名・期間1件まで | 無料 |
| Pro | スタッフ・期間 無制限＋並べ替え・テンプレート・名前色 | 500円/月・店舗 |
| Premium | Pro の全機能＋シフト作成タブ（調整・休憩/属性管理・PDF出力） | 2,980円/月・店舗 |

---

## 設計原則（変えない前提）

これらは技術的な好みではなく、プロダクトの性質から来る制約である。破るときは VISION から見直す。

1. **スタッフ側に登録を求めない** — URLトークンだけで提出できる状態を維持する。
2. **ビルドステップを持たない** — Babel Standalone によるブラウザ側トランスパイル。`import`/`export` を使わない。
   デプロイは GitHub Pages への push だけで完結させる。
3. **スタイルは inline style のみ** — 外部CSSファイル・CSSモジュールを追加しない。
4. **モバイルファースト** — `input`/`select`/`textarea` の `fontSize` は16px以上（iOS Safari のズーム防止）。
5. **データを勝手に失わない** — Firebase の `set()` によるコレクション全体上書きを禁止する。削除は二段階（`archived/` 経由）。
6. **権限は店舗単位で分離する** — 管理キー（adminKey）方式。shopId を知っているだけでは管理操作できない。

詳細な禁止事項は [RULES.md](RULES.md) を正とする。

---

## バグチェックループ完了基準

`/bug-check` は、以下をすべて満たした時点で完了とする。

- [ ] **🔴 クリティカルが 0 件**（未修正のものが残っていない。デプロイ待ちは「修正済み・デプロイ待ち」として明記されていれば可）
- [ ] **🟡 中程度が 0 件**、または残っているものすべてに「未修正の理由」が書かれている
      （仕様判断が必要／再現手段がない／ユーザー確認が必要／ループの権限外、のいずれか）
- [ ] `npm test` が全パスする
- [ ] `npx eslint app-*.js` が **0 errors**（warnings は許容）
- [ ] `node --check functions/index.js` が通る（functions を変更した場合）
- [ ] RULES.md のスキャン項目に違反がない（DEV_MODE の式・subs の全体set・全件読み・SRI・読み込み順・fontSize）
- [ ] 各修正に「原因だった証拠」を示せる（推測で直したものを完了と呼ばない）
- [ ] 未検証の項目が「未検証」と明記されている

**🟢 軽微は完了基準に含めない。** リストアップして次回へ申し送る。

## 機能実装ループ完了基準

`/shifty-feature` は、以下をすべて満たした時点で完了とする。

- [ ] BACKLOG.md のそのタスクの**受け入れ条件がすべて ✅**、または未達の条件に理由が書かれている
- [ ] `npm test` 全パス・`npx eslint app-*.js` 0 errors
- [ ] RULES.md の禁止事項に触れていない
- [ ] 実装した箇所以外に副作用がない（新たな 🔴🟡 を生んでいない）
- [ ] develop にコミット済み（**main へのマージはユーザー確認後**）
- [ ] BACKLOG.md の該当タスクが「完了済みタスク」へ移動している

---

## やらないと決めたこと

判断を蒸し返さないために、理由と再着手条件を残す。

| 項目 | 判断日 | 理由 | 再着手条件 |
|---|---|---|---|
| React Native（Expo）でのアプリ化 | 2026-07-04 | Web版のみで要件を満たせる。ストア審査・年会費・UI全書き直しのコストが見合わない | ストア配信でしか取れない要件（プッシュ通知必須等）が事業上必要になったとき |
| Vite + TypeScript へのフル移行 | 2026-07-04 | Expo 見送りにより前提が消滅。ESLint CI（`efb7c83`）で未定義参照バグは検出できている | ビルドレス構成が限界になったとき（ファイル分割で対応できない規模になる、等） |
| 管理者パスワード認証（AdminLogin） | 2026-07-07 | 管理キー（adminKey）方式へ置き換え済み。パスワードは店舗データに平文で残るため廃止 | なし（後退させない） |

---

## 現在の重点（2026-08-10 時点）

1. **課金経路の健全化** — Pro→Premium のアップグレードが旧契約を解約せず別契約を作るため、
   1店舗が2契約を持ちうる。Webhook 側にガードを積み増す対症療法が続いており、根治が最優先。
   （バグチェック #64・#65 参照）
2. **特定商取引法に基づく表記の掲出** — 有料課金を実施している以上、表記義務がある。BACKLOG の 🔴。
3. **データ保存上限の運用開始** — 36ヶ月超の期間データ削除を dry-run から本有効化へ。
